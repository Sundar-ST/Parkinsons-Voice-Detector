# Core runtime (flexible ranges for broader compatibility)
Flask>=2.0,<3
numpy>=1.22,<2
pandas>=1.3,<2
scikit-learn>=1.0,<2
joblib>=1.1,<2

# Audio / signal processing (flexible ranges)
librosa>=0.8,<1
scipy>=1.6,<2
resampy>=0.2.2,<1
audioread>=2.1,<3
soundfile>=0.12,<1
numba>=0.53,<1

# Live recording (mvp_core.py uses sounddevice)
sounddevice>=0.4,<1

# Parselmouth: try installing from PyPI (pip will fetch a matching wheel if available)
# Parselmouth (minimum supported)
praat-parselmouth>=0.4.6

# Note: Parselmouth distribution is platform- and Python-ABI-specific.
# If pip cannot find a compatible wheel for your Python, use the conda
# approach (see README) or install a matching wheel manually (see README).

# Optional (uncomment if needed)
# matplotlib>=3.5,<4
# jupyter
# pyaudio  # only if you switch to PyAudio-based recording
# gunicorn>=20,<22  # production server; use instead of Flask dev server for production deployments
